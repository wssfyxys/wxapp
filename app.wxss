/* app.wxss —— 全局样式,所有页面共享 */
/* 引入设计令牌(颜色/字体/圆角/组件类),页面开发必须引用这些变量与类,详见 docs/DESIGN.md */
@import "styles/tokens.wxss";

page {
  background-color: var(--color-canvas);
  font-size: 32rpx;
  color: var(--color-ink);
}

.container {
  padding: 40rpx;
  box-sizing: border-box;
}
